# Provider 类型：openai（默认，兼容 DeepSeek/Moonshot 等OpenAI 协议厂商）| ollama（本地模型，默认 baseUrl=http://localhost:11434/v1）
MINIHARNESS_PROVIDER=openai

# MiniHarness API Key
MINIHARNESS_API_KEY=sk-xxxx
MINIHARNESS_BASE_URL=https://api.deepseek.com
MINIHARNESS_MODEL=deepseek-chat

MINIHARNESS_MCP_SERVERS=fs:npx -y @modelcontextprotocol/server-filesystem .;mem:npx -y @modelcontextprotocol/server-memory|MEMORY_FILE_PATH=${workspace}/.anvil/mcp-memory.jsonl
# MCP 工具服务器（可选）。格式：<名字>:<启动命令> [参数...] [|KEY=VAL KEY=VAL...]
#   - 多个服务器用分号 ; 分隔
#   - 竖线 | 后是透传给服务器的环境变量（API key、存储路径等），键值对之间用空格分隔
#   - ${workspace} 会替换为当前工作目录（用于需要绝对路径的环境变量）
#   - 工具注册名规则：mcp__<名字>__<工具名>，如 fs 的 read_file 注册为 mcp__fs__read_file
#   - 服务器名字只能用字母、数字、下划线、中划线；可用 npx/uvx 等方式免安装启动
# 写代码推荐组合（filesystem 提供代码搜索/目录树/文件编辑，memory 提供跨会话记忆，落盘在 .anvil/ 内）：
# MINIHARNESS_MCP_SERVERS=fs:npx -y @modelcontextprotocol/server-filesystem .;mem:npx -y @modelcontextprotocol/server-memory|MEMORY_FILE_PATH=${workspace}/.anvil/mcp-memory.jsonl
# 其他常用服务器：
#   网页抓取（Python，需先 pip install uv）：fetch:uvx mcp-server-fetch
#   需要 API key 的服务器：|BRAVE_API_KEY=xxx 这样透传
